# Add these dependencies to your existing pubspec.yaml
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  flutter_secure_storage: ^8.0.0
  web_socket_channel: ^2.3.0
  qr_code_scanner: ^1.0.0
  fl_chart: ^0.60.0
  provider: ^6.0.5
  mqtt_client: ^9.6.1
  shared_preferences: ^2.1.1

  # NEW: Foreground/background task support
  flutter_foreground_task: ^5.5.0